# Copy to backend.tfvars and fill in from bootstrap outputs:
#   terraform -chdir=../../../bootstrap output state_bucket_name
#   terraform -chdir=../../../bootstrap output state_bucket_region
bucket = "myorg-databricks-tf-state"
region = "us-east-1"
